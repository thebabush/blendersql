# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Build
build/
dist/
*.egg-info/

# Vendored wheels — only the macOS arm64 dev wheel is committed; the rest are
# fetched by scripts/fetch_wheels.py.
wheels/*.whl
!wheels/apsw-3.53.1.0-cp313-cp313-macosx_11_0_arm64.whl

# OS / editor
.DS_Store
.vscode/
.idea/

# Blender backups inside the repo (if any)
*.blend1
*.blend@

# Test artifacts
tests/.server_info.json
tests/.blender_user_extensions/
tests/fixtures/test_scene.blend
data/
.env
nocommit/

# Bench / comparison experiments live outside this repo for now (likely
# moving to a sibling repo once the shape stabilises — see issues #1-#6).
experiments/
